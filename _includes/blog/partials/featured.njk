    <div class="site-section bg-light home-first-section">
      <div class="container">
        <div class="row align-items-stretch retro-layout-2">
          <div class="col-md-4">
            {% set fp = collections.featuredArticles[1] %}
            <a href="{{ fp.url }}" class="h-entry mb-30 v-height gradient" style="background-image: url('{{ fp.data.image }}');">
              
              <div class="text">
                <h2>{{ fp.data.title}}</h2>
                <span class="date">{{ fp.date | dateReadable }}</span>
              </div>
            </a>

            {% set fp = collections.featuredArticles[2] %}
            <a href="{{ fp.url }}" class="h-entry v-height gradient" style="background-image: url('{{ fp.data.image }}');">
              
              <div class="text">
                <h2>{{ fp.data.title}}</h2>
                <span class="date">{{ fp.date | dateReadable }}</span>
              </div>
            </a>
          </div>
          <div class="col-md-4">
            {% set fp = collections.featuredArticles[0] %}
            <a href="{{ fp.url }}" 
               class="h-entry img-5 h-100 gradient" 
               style="background-image: url('{{ fp.data.image }}');">
              <div class="text">
                <div class="post-categories mb-3">
                  <span class="post-category bg-primary">
                    {{ fp.data.category }}
                  </span>
                </div>
                <h2>{{ fp.data.title }}</h2>
                <span class="date">{{ fp.date | dateReadable }}</span>
              </div>
            </a>
          </div>
          <div class="col-md-4">
            {% set fp = collections.featuredArticles[3] %}
            <a href="{{ fp.url }}" class="h-entry mb-30 v-height gradient" style="background-image: url('{{ fp.data.image }}');">
              
              <div class="text">
                <h2>{{ fp.data.title}}</h2>
                <span class="date">{{ fp.date | dateReadable }}</span>
              </div>
            </a>
            {% set fp = collections.featuredArticles[4] %}
            <a href="{{ fp.url }}" class="h-entry v-height gradient" style="background-image: url('{{ fp.data.image }}');">
              
              <div class="text">
                <h2>{{ fp.data.title}}</h2>
                <span class="date">{{ fp.date | dateReadable }}</span>
              </div>
            </a>
          </div>
        </div>
      </div>
    </div>
